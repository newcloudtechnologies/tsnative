export namespace A {
    export namespace B {
        export namespace C {
            export enum Enum {
                ENTRY1
            }

            export function foo() {
                return Enum.ENTRY1;
            }
        }
    }
}
